{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "EventStudyRow",
  "type": "object",
  "required": ["event_id", "ticker", "event_date", "window", "return"],
  "properties": {
    "event_id": {"type": "string"},
    "ticker": {"type": "string"},
    "event_date": {"type": "string"},
    "window": {"type": "string"},
    "return": {"type": "number"}
  }
}
